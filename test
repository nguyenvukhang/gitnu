#!/usr/bin/env sh

GITNU_TEST_DIR=/tmp/gitnu_rust

cleanup() {
  rm -rf $GITNU_TEST_DIR
}
trap cleanup EXIT
cleanup
mkdir -p $GITNU_TEST_DIR

cargo build || exit 1
GITNU_TEST_DIR=$GITNU_TEST_DIR cargo test
