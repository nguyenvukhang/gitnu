name: ci

on:
  pull_request:
  push:
    branches:
      - "main"
      - "dev"

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: build 📦
        run: cargo build
      - name: run tests 🚀
        run: cargo test
